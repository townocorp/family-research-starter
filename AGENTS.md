# Starter repository instructions

This checkout is reusable tooling, not a family's private research folder.
Never add real records, personal examples, credentials or family findings here.
Use neutral placeholders and synthetic Person P001/P002 examples only.

The canonical workflow is [docs/research-process.md](docs/research-process.md).
The included skills live in `.claude/skills`; keep their folder/name metadata
consistent and their relative guide links valid. Do not add automatic shell
pre-approval, dynamic command injection or global configuration changes.

Tools require an explicit initialised private project. Do not infer one from
the environment, Git root, neighbouring directories or account identity.
Preserve both canonical-store choices. Evidence assessments need a stated
claim, rationale, supporting/opposing links and first-hand/source/inference
distinction; no model voting or implicit promotion of facts.

Use the documented offline quality command after changes. Missing dependencies
are errors, not skipped checks. Tests create their own synthetic temporary
projects and media; they do not use live sites, browsers or personal projects.

Do not commit, push, share, invite collaborators, change repository visibility
or add a software licence claim without explicit owner approval. See
[docs/privacy-and-sharing.md](docs/privacy-and-sharing.md).
